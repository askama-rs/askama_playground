trunk-version = "^0.21.7"

[build]
target = "index.html"
dist = "dist"
public_url = "./"
inject_scripts = true
minify = "on_release"

[tools]
sass = "1.85.1"
wasm_bindgen = "0.2.100"
wasm_opt = "version_122"

[clean]
cargo = true
